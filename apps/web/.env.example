# apps/web env (example)
# Copy to `apps/web/.env.local` and fill values as features are implemented.

# Backend (Next.js / Better Auth)
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=replace_with_32+_char_secret________________

# Database (Neon Postgres or local Postgres)
DATABASE_URL=postgres://user:password@localhost:5432/avolice

# OAuth providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common

# Avolice tokens (mobile bearer access JWT)
AVOLICE_JWT_SECRET=replace_with_32+_char_secret________________
AVOLICE_ACCESS_TOKEN_TTL_SECONDS=900

