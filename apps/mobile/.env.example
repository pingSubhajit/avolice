# apps/mobile env (example)
# Copy to `apps/mobile/.env` and fill values as features are implemented.

# Mobile (Expo)
# Base URL of `apps/web` backend (where the API routes live)
EXPO_PUBLIC_WEB_BASE_URL=http://localhost:3000
